//Exercise 8 Sum of Multiples of 3 and 5
console.log(sum(10);

// Multiples of 
function sum(limit){
  const sum = 0;

for (let i = 0; i <= limit; i++) 
if (i % 3 === 0 || i % 5 === 0)
sum += i;

return sum;
}
